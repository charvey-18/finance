# AGENTS

## Documentation Hygiene
- Keep active memory docs lean and current-state focused.
- Store verbose history only under `docs/history/project-memory/<YYYY-MM-DD>/` when needed.
- Update `CONTEXT.MD`, `ROADMAP.MD`, and `HANDOFF.md` on each substantive prompt touching this workspace.
- Use concise decision logs with UTC ISO timestamps.
